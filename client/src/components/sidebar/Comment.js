import styled from 'styled-components';
const Comment = ({ userComment }) => {
  console.log(userComment);
  console.log(Object.keys(userComment));
  return <Wrapper></Wrapper>;
};

export default Comment;

const Wrapper = styled.div``;
